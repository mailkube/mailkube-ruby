# Run `pre-commit install && pre-commit install --hook-type commit-msg` after cloning.
# Requires Ruby with the bundle installed, and Node (for jscpd) on PATH.
repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v5.0.0
    hooks:
      - id: end-of-file-fixer
      - id: trailing-whitespace
      - id: check-yaml
      - id: check-merge-conflict
      - id: check-added-large-files

  - repo: local
    hooks:
      - id: rubocop
        name: rubocop (lint + format)
        entry: bundle exec rubocop --autocorrect
        language: system
        types: [ruby]
        pass_filenames: false
      - id: steep
        name: steep (type check)
        entry: bundle exec steep check
        language: system
        types: [ruby]
        pass_filenames: false
      # The rubocop hook above already covers examples — it reads .rubocop.yml, which no longer
      # excludes them. This is the syntax floor CI's `examples` job runs, mirrored locally.
      - id: examples-parse
        name: examples parse (ruby -c)
        entry: ruby -c
        language: system
        files: ^examples/.*\.rb$

  # Language-agnostic DRY gate — identical config in every mailkube repo.
  - repo: local
    hooks:
      - id: jscpd
        name: jscpd (copy-paste detector)
        entry: npx --yes jscpd@4 --config .jscpd.json .
        language: system
        pass_filenames: false
      - id: jscpd-examples
        name: jscpd (examples, higher threshold)
        entry: npx --yes jscpd@4 --config .jscpd.examples.json examples/
        language: system
        pass_filenames: false

  # Enforce Conventional Commits on the commit message (matches the org house style).
  - repo: https://github.com/alessandrojcm/commitlint-pre-commit-hook
    rev: v9.22.0
    hooks:
      - id: commitlint
        stages: [commit-msg]
        additional_dependencies:
          - "@commitlint/config-conventional"
